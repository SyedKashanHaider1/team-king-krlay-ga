# AI Marketing Command Center — Environment Variables
# Copy this file to .env and fill in your values

# ── Server ──────────────────────────────────────────
# Strong secret key for JWT signing (CHANGE THIS!)
SECRET_KEY=change-this-to-a-very-long-random-string-in-production

# Flask debug mode (set to false in production)
FLASK_DEBUG=true

# Server port (default: 5000)
PORT=5000

# ── Google OAuth ─────────────────────────────────────
# Get from: https://console.cloud.google.com/ → APIs & Services → Credentials
# Leave as-is to use Demo mode only
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com

# ── Database ─────────────────────────────────────────
# Path to SQLite database file (auto-created on first run)
DATABASE_PATH=./data/marketing.db

# ── JWT Settings ─────────────────────────────────────
# Token expiry in hours (default: 24)
JWT_EXPIRY_HOURS=24

# ── Frontend ─────────────────────────────────────────
# Frontend URL for CORS (update if deploying)
FRONTEND_URL=http://localhost:5000

# ── AI Service (Optional — for real LLM integration) ─
# Uncomment and fill to use real AI:
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# AI_MODEL=gpt-4
