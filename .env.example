# AI Marketing Command Center — Environment Variables
# Copy this file to .env and fill in your values

# ── Server ──────────────────────────────────────────
# Strong secret key for JWT signing (CHANGE THIS!)
SECRET_KEY=change-this-to-a-very-long-random-string-in-production

# Flask debug mode (set to false in production)
FLASK_DEBUG=false

# Server port (default: 5000, Railway sets this automatically)
PORT=5000

# Railway environment (set to 'production' when deployed)
RAILWAY_ENVIRONMENT=production

# ── Database ─────────────────────────────────────────
# Path to SQLite database file (auto-created on first run)
# For Railway PostgreSQL, this will be overridden by DATABASE_URL
DATABASE_PATH=./data/marketing.db

# ── JWT Settings ─────────────────────────────────────
# Access token expiry in minutes (default: 15)
JWT_ACCESS_EXPIRY_MINUTES=15

# Refresh token expiry in days (default: 7)
JWT_REFRESH_EXPIRY_DAYS=7

# ── Frontend ─────────────────────────────────────────
# Frontend URL for CORS (update with your Railway frontend URL)
FRONTEND_URL=https://your-frontend-url.railway.app

# ── AI Service (Optional — for real LLM integration) ─
# Gemini (Google Generative Language API)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY
GEMINI_MODEL=gemini-1.5-flash
